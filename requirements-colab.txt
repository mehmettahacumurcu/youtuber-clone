# Installed on the Colab runtime each session. Matches the tested-good combo.
# gradio is intentionally omitted (the review UI runs locally, not on Colab).
numpy>=1.24,<2
pyannote-audio>=3.1,<3.3
speechbrain<1.0
huggingface-hub>=0.25,<1.0
pydantic-settings>=2.14.1
pyyaml>=6.0.3
silero-vad>=6.2.1
soundfile>=0.13.1
torch>=2.6,<2.9
torchaudio>=2.6,<2.9
# torchvision must match torch (0.21=2.6, 0.22=2.7, 0.23=2.8) or pyannote dies with
# "operator torchvision::nms does not exist". Pinning it lets pip resolve a matched pair.
torchvision>=0.21,<0.24
yt-dlp>=2026.6.9
faster-whisper>=1.2.1
transformers>=4.45
sentencepiece>=0.2.1
